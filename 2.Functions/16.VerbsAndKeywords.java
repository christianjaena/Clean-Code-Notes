/**
	VERBS AND KEYWORDS
*/

// Monads: Verb + Noun
write(name);
// whatever the name is, is being written
writeField(name);

// For functions:
assertEquals();
// is better than
assertExpectedEqualsIsActual(expected, actual);
// this strongly mitigates the problem of having to remember the ordering of the arguments.
